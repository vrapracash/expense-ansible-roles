- name: nginx install and run
  hosts: nginx
  #hosts: all
  become: yes
  serial: 3

  tasks:
  - name: Install nginx
    ansible.builtin.package:
      name: nginx
      state: present

  - name: Start nginx
    ansible.builtin.service:
      name: nginx
      start: started
      enabled: yes
       